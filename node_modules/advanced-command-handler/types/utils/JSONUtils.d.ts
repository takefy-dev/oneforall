export declare type JSONLike = {
    [k: string]: any[] | number | string | JSONLike;
};
/**
 * @param path - The path of the JSON file.
 * @param content - The content to save.
 * @returns - True if operation has successfully worked.
 */
export declare function saveJSON(path: string, content: any): boolean;
/**
 * Reads a JSON through its path.
 *
 * @remarks
 * Prefer using `import` or `require`.
 *
 * @param path - The path to the JSON file.
 * @returns The JSON.
 */
export declare function readJSON(path: string): any;
/**
 * Reads a JSON through its path.
 *
 * @remarks
 * Prefer using `import` or `require`.
 *
 * @typeParam O - The type of the JSON if any.
 * @param path - The path to the JSON file.
 * @returns The JSON.
 */
export declare function readJSON<O extends JSONLike | any[]>(path: string): O;
