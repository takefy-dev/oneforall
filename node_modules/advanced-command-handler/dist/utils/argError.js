"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const discord_js_better_embed_1 = require("discord.js-better-embed");
exports.default = (message, error, command) => {
    const embed = discord_js_better_embed_1.BetterEmbed.fromTemplate('complete', {
        client: message.client,
        color: 0xee2200,
        title: 'Argument error :',
        description: error,
    });
    if (command.usage)
        embed.addField('Syntax :', command.usage);
    if (message.client.channels.cache.has(message.channel.id))
        return message.channel.send(embed);
    else
        throw new Error(`The channel ${message} is not valid, an ID is expected, if an ID has been entered, then the bot does not have this channel.`);
};
