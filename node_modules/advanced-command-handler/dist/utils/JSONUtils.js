"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.readJSON = exports.saveJSON = void 0;
const fs = __importStar(require("fs"));
const Logger_js_1 = require("./Logger.js");
/**
 * @param path - The path of the JSON file.
 * @param content - The content to save.
 * @returns - True if operation has successfully worked.
 */
function saveJSON(path, content) {
    if (!fs.existsSync(path)) {
        Logger_js_1.Logger.warn(`Cannot save JSON content to '${path}', file not found.`);
        return false;
    }
    if ((typeof content === 'string' && content.length === 0) || JSON.stringify(content, null, 4).length === 0) {
        Logger_js_1.Logger.warn(`Cannot save JSON content to '${path}', content is empty.`);
        return false;
    }
    fs.writeFile(path, JSON.stringify(content, null, 4), err => {
        if (err)
            Logger_js_1.Logger.error(`Cannot save JSON content to '${path}', error:\n${err.stack}`);
        return false;
    });
    return true;
}
exports.saveJSON = saveJSON;
/**
 * Reads a JSON through its path.
 *
 * @remarks
 * Prefer using `import` or `require`.
 *
 * @typeParam O - The type of the JSON if any.
 * @param path - The path to the JSON file.
 * @returns The JSON.
 */
function readJSON(path) {
    const bufferedData = fs.readFileSync(path);
    return JSON.parse(bufferedData.toString('utf8'));
}
exports.readJSON = readJSON;
